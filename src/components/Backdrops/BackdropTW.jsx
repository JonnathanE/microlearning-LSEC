import Loader from '../Loader/Loader';
import Modal from '../Modal/Modal';

const BackdropTW = () => {
    return (
        <Modal>
            <Loader />
        </Modal>
    );
}

export default BackdropTW;
